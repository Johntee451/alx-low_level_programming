#include "main.h"

/**
 * _strcat - concatintes strings.
 * @dest: parameter destinatiion.
 * @src:parameter sourse.
 * Return: strr.
 */

char *_strcat(char *dest, char *src)
{
	char *strr;

	strr = strcat(dest, src);

	return (strr);
}
