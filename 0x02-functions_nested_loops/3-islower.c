#include "main.h"

/**
 * _islower - check if argument is lowercase or not.
 * @c: accepts interger value.
 * @r: stores the results.
 * return: Always 0.
 */

int _islower(int c)
{
	int r;

	r = _islower(c);

	putchar(r + '0');

	putchar('\n');

	return (0);
}
