#include "main.h"

/**
 * _strcpy - copy string to each other.
 * @dest: the destination.
 * @src: the source.
 * Return: dest.
 */

char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
